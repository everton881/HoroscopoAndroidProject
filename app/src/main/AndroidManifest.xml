<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.everton.horoscopo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".database.HoroscopoProvider"
            android:authorities="everton.android.horoscopo"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".HoroscopoDetailActivity"
            android:label="@string/title_activity_horoscopo_item"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".itemListaActivity"
            android:label="@string/title_activity_item_lista"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>